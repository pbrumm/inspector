<h1>All items</h1>
<table>
	<thead>
		<th>Name</th>
		<th>Survey</th>
	</thead>

	<tbody>
		<% @items.each do |item| %>
			<tr>
				<td><%= item.name %></td>
				<td><%= Survey.find(item.survey_id).name %></td>
			</tr>
		<% end %>
	</tbody>

</table>